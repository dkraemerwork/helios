/**
 * Port of {@code com.hazelcast.map.impl.query.QueryResultUtils}.
 *
 * Utility class for transforming QueryResult objects.
 */
export class QueryResultUtils {
    private constructor() {
        throw new Error('QueryResultUtils is a utility class');
    }
}
