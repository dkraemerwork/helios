/** Special attribute name representing the entry key. */
export const KEY_ATTRIBUTE_NAME = '__key';

/** Special attribute name representing the entry value itself. */
export const THIS_ATTRIBUTE_NAME = 'this';
